{
  "name": "ourverse-monorepo",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "start": "npm --prefix backend run start",
    "dev": "npm --prefix backend run dev",
    "tunnel": "npm --prefix backend run tunnel"
  }
}
