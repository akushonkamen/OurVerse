# ==========================================
# OurVerse Photo Sharing App - Configuration
# ==========================================

# ==========================================
# OurVerse Photo Sharing App - Railway Deployment Configuration
# ==========================================

# Database Configuration (Railway会注入这些变量)
# MONGODB_URI - Railway自动提供
# MONGO_USERNAME - Railway数据库用户名
# MONGO_PASSWORD - Railway数据库密码

# Authentication & Security (这些需要在Railway控制台手动设置)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
BCRYPT_SALT_ROUNDS=10
SESSION_SECRET=your-session-secret-key

# GitHub OAuth Configuration (需要在Railway控制台设置)
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here
# GITHUB_CALLBACK_URL: Railway会自动生成，使用Railway提供的域名

# Railway Environment Variables (Railway自动注入)
# PORT - Railway自动分配
# NODE_ENV - 自动设为production
# DOMAIN - Railway自动提供
# PROTOCOL - Railway自动提供 (https)
# FRONTEND_URL - Railway自动提供

# Amap (高德地图) API Configuration (需要在Railway控制台设置)
AMAP_WEB_API_KEY=your_amap_web_api_key_here
AMAP_REST_API_KEY=your_amap_rest_api_key_here
AMAP_SECURITY_CODE=your_amap_security_code_here

# Application Configuration
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp
UPLOADS_DIR=uploads
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Business Logic Parameters
DAILY_UPLOAD_LIMIT=3
MAX_DISTANCE_VERIFICATION=50
LOCATION_VERIFICATION_RADIUS=50

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Session Configuration
SESSION_COOKIE_MAX_AGE=86400000

# Railway Specific Configuration
# Railway会自动处理HTTPS和负载均衡，无需额外配置
# CORS_ORIGIN=* # 在Railway上可能需要根据域名调整
